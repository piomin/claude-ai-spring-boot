apiVersion: v1
kind: Service
metadata:
  name: claude-ai-spring-boot
  labels:
    app: claude-ai-spring-boot
spec:
  type: LoadBalancer
  selector:
    app: claude-ai-spring-boot
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
